{
  "name": "notifier-bot",
  "version": "1.0.0",
  "description": "This is the bot that tracks the vulgar, hate and spam comments done by the specific user in yt or hashnode blogs.",
  "main": "server.js",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "bot": "node --watch --env-file=config.env index.js"
  },
  "dependencies": {
    "mongoose": "^8.0.3",
    "nodemailer": "^6.9.8"
  }
}
